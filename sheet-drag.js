(()=>{function mount(){const sheet=document.querySelector('.elma-sheet'),grab=document.querySelector('.elma-grab');if(!sheet||!grab||sheet.dataset.dragReady)return;sheet.dataset.dragReady='1';const style=document.createElement('style');style.textContent=`.elma-grab{height:22px!important;width:72px!important;margin:-5px auto 3px!important;display:flex!important;align-items:center!important;justify-content:center!important;touch-action:none!important;cursor:grab!important}.elma-grab:after{content:"";display:block;width:38px;height:4px;border-radius:99px;background:#777a80;transition:width .18s ease,background .18s ease}.elma-sheet.elma-dragging{transition:none!important;will-change:transform,opacity}.elma-sheet.elma-dragging .elma-grab:after{width:48px;background:#b4b6bb}.elma-sheet.elma-sheet-dismiss{pointer-events:none;transform:translate3d(0,calc(100% + 130px),0)!important;opacity:0!important;transition:transform .32s cubic-bezier(.22,.8,.24,1),opacity .24s ease!important}`;document.head.appendChild(style);let startY=0,lastY=0,lastT=0,velocity=0,dragging=false;const move=y=>{if(!dragging)return;const dy=Math.max(0,y-startY),now=performance.now(),dt=Math.max(1,now-lastT);velocity=(y-lastY)/dt;lastY=y;lastT=now;sheet.style.transform=`translate3d(0,${dy}px,0)`;sheet.style.opacity=String(Math.max(.12,1-dy/360))};const end=()=>{if(!dragging)return;dragging=false;sheet.classList.remove('elma-dragging');const m=(sheet.style.transform.match(/,([\d.]+)px/)||[])[1],dy=Number(m||0);if(dy>95||velocity>.55){sheet.style.transform='';sheet.style.opacity='';requestAnimationFrame(()=>sheet.classList.add('elma-sheet-dismiss'))}else{sheet.style.transition='transform .28s cubic-bezier(.22,.8,.24,1),opacity .2s ease';sheet.style.transform='translate3d(0,0,0)';sheet.style.opacity='1';setTimeout(()=>{sheet.style.transition='';sheet.style.transform='';sheet.style.opacity=''},300)}};grab.addEventListener('pointerdown',e=>{if(sheet.classList.contains('elma-sheet-dismiss'))return;dragging=true;startY=lastY=e.clientY;lastT=performance.now();velocity=0;sheet.classList.add('elma-dragging');grab.setPointerCapture?.(e.pointerId);e.preventDefault()});grab.addEventListener('pointermove',e=>move(e.clientY));grab.addEventListener('pointerup',end);grab.addEventListener('pointercancel',end)}const obs=new MutationObserver(mount);obs.observe(document.documentElement,{childList:true,subtree:true});mount()})();